enum Foo {
    Bar (int, int),
    Zot {
        int x;
        int y;
        int z;
    },
    Hoge,
    Fuga,
};

Foo foo = Foo::Bar(1, 2);

int main() {
    if let (Foo::Zot { x, y, z } = foo) {
        return x + y + z;
    }else{
        return 0;
    }
}