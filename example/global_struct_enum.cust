enum Foo {
    Bar (int, int),
    Zot {
        int x;
        int y;
    },
};

Foo foo = Foo::Zot {
    x: 10;
    y: 20;
};

int main() {
    if let (Foo::Zot { x, y } = foo) {
        return x + y;
    }else{
        return 0;
    }
}