enum Foo {
    Bar (int, int),
    Zot {
        int x;
        int y;
        int z;
    },
    Hoge,
    Fuga,
};

Foo foo = Foo::Zot{ x: 1; y: 2; z: 3;};

int main() {
    if let (Foo::Zot { x, y, z } = foo) {
        return x + y + z;
    }else{
        return 0;
    }
}