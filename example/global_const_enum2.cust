enum SomeEnum {
    Foo (int, int),
    Bar {
        int x;
        int y;
        int z;
    },
    Zot,
};

SomeEnum some_enum = SomeEnum::Bar{ x: 1; y: 2; z: 3;};

int main() {
    if let (SomeEnum::Bar { x, y, z } = some_enum) {
        return x + y + z;
    }else{
        return 0;
    }
}