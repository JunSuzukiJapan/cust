int printf(char* format, ...);

$<int, int> tpl = $(1, 2);

int main(){
    if let ($(x, y) = tpl) {
        printf("x: %d, y: %d\n", x, y);
        return x + y;
    } else {
        return 0;
    }
}